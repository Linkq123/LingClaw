// ── Brand ──
export const DEFAULT_BRAND_AVATAR = 'branding/avatar.png';
export const DEFAULT_WELCOME_LOGO = 'branding/logo-wordmark.png';

// ── Scroll ──
export const AUTO_SCROLL_THRESHOLD = 88;

// ── Markdown streaming ──
export const SOFT_SPLIT_MIN_CHARS = 72;
export const SOFT_SPLIT_MAX_CHARS = 160;
export const SOFT_SPLIT_TAIL_MIN_CHARS = 18;

// ── Connection ──
export const MAX_RECONNECT_ATTEMPTS = 50;

// ── ReAct status ──
export const MIN_REACT_ANALYZE_VISIBLE_MS = 180;
export const MIN_REACT_ACT_VISIBLE_MS = 420;
export const MIN_REACT_OBSERVE_VISIBLE_MS = 650;
export const MAX_REACT_QUEUED_PHASES = 2;

// ── History ──
export const HISTORY_RENDER_LIMIT = 50;

// ── Input ──
export const INPUT_HISTORY_MAX = 10;
